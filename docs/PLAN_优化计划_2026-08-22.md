# bilibili-learn Skill 优化实施计划

> **日期**：2026-08-22
> **目标**：基于 3 次实战（收藏夹总览 + 6 视频报告 + Transformer 34P 课程）的教训，系统性优化 bilibili-learn skill
> **状态**：✅ 已确认（2026-08-22），进入执行阶段

## 进度跟踪

- [x] **第一批（2026-08-22 完成）**：A0 名词注释（glossary.py + 种子术语库 + SKILL.md R11 + 模板）、B1 内容错乱检测（中文 2-gram 零命中/歌词/行密度）、B2 回归测试。单测 12/12，真实样本 6/6。
- [x] **第二批（2026-08-22 完成）**：C1 `bili doctor`（cookie/GPU/whisper/模型缓存/输出目录五项自检 + 修复提示）、C2 GPU DLL 自动注入（_ensure_nvidia_paths）、B3 覆盖自检（glossary.py check 集成）。单测 19/19，doctor 实测全绿。
- [x] **第三批（2026-08-22 完成）**：C3 字幕缓存（subcache.py，实测第二次运行命中 cache）、D3 限流退避（--interval 参数 + retry_with_backoff 指数退避）、D2 `bili merge`（mergeutil.py，单测覆盖跳过/复制）。单测 26/26。
- [x] **第四批（2026-08-22 完成）**：D1 `bili favs-scan`（favs.py：分页拉全量+元信息/主题过滤/优先级排序/失效统计/快照）。单测 31/31，真实收藏夹实测：139 个拉取、86 个 AI 命中、88 个失效统计。

---

## 用户决策记录

1. **注释形式**：每个知识点的名词注释**写在该知识点末尾**（独立小节），不写在文末
2. **术语库维护**：agent 写报告时**自动沉淀**（新术语自动写入 glossary.json）
3. **错乱字幕处置**：可疑字幕**直接降级 Whisper**（不询问用户）
4. **实施范围**：四批全部实施，**测试不可省略**（TDD：先测试后实现）

---

## 一、核心新需求（用户提出）

### A0. 名词注释机制（书籍式脚注）

**问题**：报告知识点中出现大量无前置提及的专业名词（如 teacher forcing、残差连接、因果语言模型、OOV、teacher forcing…），纯文本学习需反复查询，是文本类学习最大弊端。

**方案**（四层设计）：
1. **内联首注**：知识点正文中首次出现的专业名词加注释标记，如 `*teacher forcing*¹`（¹ 引用文末注释表）
2. **文末名词注释表**：报告末尾新增「名词注释表」章节，按首次出现顺序编号，每条 = 名词 + 一句话解释（类似书籍脚注）
3. **术语库沉淀**：skill 维护 `references/glossary.json`（术语 → 解释），跨视频复用（KV Cache/MoE/MTP/GQA 等词在多视频重复出现，写一次注释到处引用）；agent 写作时查库，未命中则新写并沉淀
4. **机械校验**：报告生成后运行 `glossary.py check`，将字幕中的术语（术语库 + 正则匹配英文术语/缩写）与报告注释表比对，输出「未注释术语清单」提示补齐

**涉及文件**：
- `SKILL.md`：新增规则 R11（名词注释硬性要求：知识点中无前置提及的名词必须注释）
- `scripts/report_template.md`：模板增加「名词注释表」章节
- `scripts/glossary.py`（新增）：术语库读写 + 注释覆盖率校验
- `references/glossary.json`（新增）：初始种子术语（从已生成的 6 份报告提取）

**验收标准**：随机抽 3 份新报告，字幕中出现的专业名词 ≥95% 在注释表中；同一术语在不同报告使用同一解释（来自术语库）。

---

## 二、正确性修复类（本次实战踩坑）

### B1. 字幕「内容错乱」检测（最高优先级）

**问题**：3 个视频的 AI 字幕内容完全错乱（靶场视频/影视台词）却通过现有校验（行数+时间覆盖只防残缺/错位，防不了内容无关）。

**方案**：`_subtitle_complete` 增加内容级启发式校验，命中即标记可疑、降级 Whisper：
- 歌词特征：`♪` 字符、相邻行重复度高
- 标题/简介关键词重叠度：视频标题核心词（分词后）在字幕中的命中率低于阈值 → 可疑
- 行密度异常：行数/时长比超出同类 P 的合理区间（如 615s 视频出现 3000+ 行）
- 越界时间戳（已修，补充单测）

**涉及文件**：`scripts/api_client.py`（`_subtitle_complete`）、新增 `scripts/tests/test_subtitle_quality.py`

**验收标准**：用本次 3 个错乱样本（靶场/影视剧/3018行越界）+ 3 个正常样本做回归，错乱样本全部判可疑，正常样本全部通过。

### B2. 多 P 校验回归测试（已修复，固化）

**问题**：`get_subtitle_text` 误传总时长导致跨 P 错乱字幕漏检（10109s 通过 615s 视频的校验）。

**方案**：修复已应用（`page_obj.duration`）；新增单测覆盖：多 P 视频中某 P 字幕末条时间超出该 P 时长 130% 时必须判不可信。

**涉及文件**：`scripts/tests/test_subtitle_quality.py`

### B3. 报告知识点覆盖自检（本次手动执行，脚本化）

**问题**：生成总报告时可能遗漏字幕中的知识点（本次发现 26 处遗漏，靠人工通读+术语比对发现）。

**方案**：`report_check.py`：从 subtitle.txt 提取关键术语（术语库+正则），与报告文本比对，输出未覆盖术语清单；支持 `--term-only` 模式（A0 的注释校验复用同一机制）。

**验收标准**：对 Transformer 课程报告运行，遗漏术语 ≤ 5%（本次人工审计后为 0，脚本应与人工一致）。

---

## 三、环境健壮性类

### C1. `bili doctor` 环境自检命令

**问题**：本次 GPU 问题排查多轮（cuBLAS 缺失、faster-whisper 1.2 API 变更、HF 镜像、cookie 过期），全靠人工诊断。

**方案**：`bili doctor` 输出诊断报告：① cookie 有效性（nav 接口）；② GPU/cuBLAS/cuDNN 可用性（缺失时给出 `pip install nvidia-cublas-cu12 nvidia-cudnn-cu12` 修复命令）；③ faster-whisper 版本与 API 兼容（progress_callback/log_progress）；④ 模型缓存与 HF 镜像建议；⑤ 输出目录可写性。

**涉及文件**：`scripts/doctor.py`（新增）、`bili.py` 注册子命令

### C2. GPU DLL 路径自动注入（固化）

**问题**：本次手动注入 `site-packages\nvidia\*\bin` 到 PATH 才启用 GPU，换环境需重来。

**方案**：`transcriber.py` 启动时自动探测 site-packages 下 nvidia DLL 路径并注入 PATH；`detect_device` 失败时给出明确修复提示。

### C3. 字幕/转写结果缓存

**问题**：重复处理同一视频会重复下载/转写（本次 P03 重转时重复下载音频）。

**方案**：按 `(bvid, cid)` 哈希缓存 subtitle.txt 与转写结果到 `scripts/cache/`；`run` 时先查缓存（可 `--no-cache` 关闭）。

---

## 四、功能拓展类

### D1. 收藏夹批处理 `favs-scan`（本次核心场景）

**问题**：skill 定位单视频深入报告，但真实工作流是「收藏夹 → 筛选主题 → 优先级分批总结」，本次全部手工完成。

**方案**：
- `bili favs-scan <fav_id> [--filter ai] [--priority]`：列出全部视频+元信息（播放/UP主/时长/简介），按关键词做主题聚类与 AI 相关性打分，输出建议处理顺序；统计失效视频（显示数与返回数差异）
- `bili favs-scan --snapshot`：记录快照，后续增量识别新收藏

**涉及文件**：`scripts/bili.py` 新增子命令 + `scripts/favs.py`

### D2. 跨天/重复目录合并

**问题**：批量处理跨午夜时按日期新建目录，同一视频分裂成两个合集目录（本次手动合并清理）。

**方案**：`run` 定位输出目录时优先复用同 bvid 已有目录（日期仅作目录名后缀）；提供 `bili merge <dir1> <dir2>` 合并命令。

### D3. 请求限流与退避

**问题**：批量 34P 时字幕接口频繁「两次不一致→降级 Whisper」。

**方案**：批量模式自动加请求间隔（默认 1.5s，可 `--interval` 调）+ 失败指数退避重试（3 次）；降低无谓的 Whisper 兜底。

---

## 五、建议实施顺序（按价值/成本比）

| 阶段 | 项目 | 理由 |
|------|------|------|
| **第一批** | A0 名词注释 + B1 错乱检测 + B2 回归测试 | 用户核心诉求 + 本次最大数据质量坑 |
| **第二批** | C1 doctor + C2 GPU 注入 + B3 覆盖自检 | 环境自愈 + 报告质量闭环 |
| **第三批** | C3 缓存 + D3 限流 + D2 目录合并 | 工程体验 |
| **第四批** | D1 favs-scan | 功能拓展（可独立规划） |

每批完成后：更新 SKILL.md 文档 + 用真实视频（取收藏夹剩余 7 个）做端到端验证。

---

## 六、待用户决策项

1. **名词注释粒度**：A0 采用「文末注释表 + 正文上标」还是「正文内联括号注」？还是两者结合（短注内联、长注入表）？
2. **术语库维护方式**：自动沉淀（agent 写报告时自动写入 glossary.json）还是人工审核后沉淀？
3. **错乱检测的保守度**：可疑字幕直接降级 Whisper（慢但稳），还是标记 warning 后询问用户（省时间但可能漏）？
4. **实施范围**：四批全部实施，还是先做第一批？
